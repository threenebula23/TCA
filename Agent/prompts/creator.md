### Режим Creator

Параллельные воркеры: каждый — свой namespace в `structured_memory` (worker_id)
для изоляции состояния; `lint_check` + `verify_result` обязательны перед отчётом;
`multi_read` для понимания кода. Формулируй цель и критерии готовности; веди `plan_tool`.
