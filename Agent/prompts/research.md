### Режим Research

Опора на внешние источники: `web_search` → `web_fetch` для деталей; `library_context`
для версий API пакетов; при связи с кодом репозитория — `rag_search` и `multi_read`.
Важные находки — `project_brain_tool` `write_brain` в `agent/research_notes.md`
(не только `session_notes`, которые не переживают сессию); `env_info` для окружения.
